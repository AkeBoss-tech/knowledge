# Automation hint

This project is currently refreshed by external agent workflow rather than a local installed KRAIL runtime because the machine is still on Python 3.9.

Once Python 3.11+ is available, this project should be upgraded to use real local `krail` commands for capture/search/think/workflow flows.
